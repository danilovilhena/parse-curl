{
  "name": "fast-parse-curl",
  "version": "1.0.3",
  "description": "Parse cURL commands into structured JSON objects",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc --noEmit && bun build ./src/index.ts --outfile ./dist/index.js --target node --format esm --minify && tsc --emitDeclarationOnly --outDir dist",
    "test": "bun test",
    "test:build": "bun run build && bun test tests/index.build.test.ts",
    "test:coverage": "bun test --coverage",
    "prepublishOnly": "bun run build"
  },
  "keywords": [
    "curl",
    "parse",
    "http",
    "request",
    "parser",
    "cli"
  ],
  "author": {
    "name": "Danilo Vilhena",
    "email": "danilo.vilhena@edu.unifor.br",
    "url": "https://github.com/danilovilhena"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/danilovilhena/fast-parse-curl.git"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.0.0"
  },
  "engines": {
    "bun": ">=1.0.0"
  }
}
